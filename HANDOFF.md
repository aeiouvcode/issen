# HANDOFF
Milestone loop: build in /home/sandbox/issen/index.html -> syntax-check module AS .mjs -> deploy via PAT bridge (vault entry "GitHub push token - aeiouvcode", kind login key password; fill into injected input on https://aeiouvcode.github.io/issen/ page - root CSP allows api.github.com; files.instinct.com CSP does NOT) -> poll live md5 LIVE-MATCH -> numeric QA via window.QA.step on live (stage+measure in ONE evaluate; freeze staged foes thinkT=999) -> security delta scan -> screenshots desktop+390px (?v= cache-bust) -> port to /home/sandbox/issen-file/src (game-main.ts=module minus three import wrapped in init(); markup/css=JSON-escaped single-line, non-ASCII as \uXXXX) -> node --check .mjs -> file build (fresh op-id) -> status -> preview smoke in depth-1 frame (sandboxed iframe: NO localStorage - all storage is try/catch guarded) -> publish (gen from status) -> state files + bridge commit -> update wake prompt -> report to parent with frames+File URL.
Workspace may be wiped between cycles: restore index.html from live URL, state from raw.githubusercontent, File via tools file checkout, notes re-clone, ref frames via api.fxtwitter.com + ffmpeg.
Browser budget: hourly-check caution applies; lease per cycle, release at end.

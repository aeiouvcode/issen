# HANDOFF
Pipeline unchanged (edit -> node --check .mjs -> PAT bridge PUT -> live md5 poll -> phone+desktop verify -> File port -> build -> preview smoke -> publish -> state -> report).
New gotchas: File preview iframe content lease expires fast - navigate to the iframe URL immediately after loading the wrapper, in one flow; if you see a bare <pre>"Content lease expired Treat or invalid"</pre>, re-issue file preview and redo. iframe src extraction: execute-js without --json prints the raw value - simplest. vendor-three.js is now the MINIFIED three build - keep source edits in game-main.ts only.
M100 candidates: ground stipple density (reference is denser/darker), grass stroke patches (broad horizontal strokes mid-field), confirm no regression from vignette change on bright scenes.

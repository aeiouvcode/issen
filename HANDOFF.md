# HANDOFF
Milestone loop: build in /home/sandbox/issen/index.html -> syntax-check module AS .mjs -> deploy via PAT bridge (vault entry "GitHub push token - aeiouvcode", kind login key password; fill into injected labeled password input on https://aeiouvcode.github.io/issen/ - root CSP allows api.github.com; files.instinct.com does NOT) -> poll live md5 LIVE-MATCH -> numeric QA via window.QA.step on live (stage+measure in ONE evaluate; freeze staged foes thinkT=999 + _parryCD=999; page rAF consumes staged state between calls) -> security delta scan -> screenshots desktop+390px (?v= cache-bust) -> port to /home/sandbox/issen-file/src (game-main.ts=module minus three import wrapped in init(); markup/css=JSON-escaped single-line, non-ASCII \uXXXX) -> node --check .mjs -> file build (fresh op-id) -> status -> preview smoke in depth-1 frame (sandboxed iframe: NO localStorage - all storage try/catch guarded) -> publish (gen from status) -> state files + bridge commit -> update wake prompt -> guidance record -> release lease -> ONE final report with frames+File URL.
Large downloads from page (canvas dataURLs): execute-js return caps - slice in 60000-char chunks, read .content (no .structured on plain strings).
WebGL exhaustion: after many heavy 3D sessions in one lease, webgl2 starts failing session-wide (fresh tabs too) - release and reacquire the lease.
Icon fonts: sandbox has NO CJK fonts - generate kanji art in the browser (Mincho available), not PIL.
Workspace may be wiped between cycles: restore index.html from live URL, state from raw.githubusercontent, File via tools file checkout, notes re-clone, ref frames via api.fxtwitter.com + ffmpeg.
White-out screenshots after long synchronous sims are usually a present-timing artifact, not a bug: run a <=80-step batch before shooting. Check gl.isContextLost() to separate true GL death (release/reacquire lease) from artifacts. startOrRetry needs a REAL click (cloud_browser click/click-at on the found ref); synthetic .click() silently no-ops. Game-over removes the HUD element entirely, so bar assertions must run mid-fight.

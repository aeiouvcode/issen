# HANDOFF
Pipeline unchanged: edit /home/sandbox/issen/index.html -> node --check module as .mjs -> PAT bridge PUT (vault: "GitHub push token - aeiouvcode", injected #pat-input on aeiouvcode.github.io page, chunk 60000, verify __b64.length before PUT) -> poll live md5 -> verify phone+desktop -> port to issen-file sources -> file build -> preview smoke (navigate tab directly to iframe URL, execute-js works there) -> publish with preview image -> state files -> report.
Gotchas: QA export line appears TWICE (n=2). rAF loop body uses `const rawDt = dt;` (spaced); QA.step uses compact. game-css.ts/markup.ts are JSON-escaped single-line exports - one literal backslash before \n and \" in raw bytes.
M99 candidates: real-device fps read via prInfo, seal-button touch targets (42px -> 48px?), chapter-select pips on phone, payload/load audit, then reference-distance work (grass/slash-trail from prior audit list).

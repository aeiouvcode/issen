# HANDOFF — 2026-09-25 3:36 AM IST (post-M107)
State: M107 live + File gen 68. Standing mandate: 50-min cycles, distance-to-reference audit, mobile-first, gauntlet (game design / product / security / overall design / presentation / UI-UX / sound), publish each passed build as Instinct File, report per-milestone to parent.
Resume: pull live index.html if workspace wiped; state files from raw.githubusercontent; File skeleton via tools file checkout.
GitHub: web UI editor flow only (credential rule - token never in sandbox); session on cloud browser config-c; index.html paste via --script-stdin (arg limit); commit via dialog; verify repo md5 then live md5 (Pages lags ~1 min).
Reference: sensonoken Unity sumi-e duel footage (/tmp/refframes/ref-*.png; refetch via api.fxtwitter.com/sensonoken/status/2100371695727198244).
Agent notes repo: https://github.com/aeiouvcode/agent-specific-notes (advisory) - consult before matching operations.
Known gotchas: no gl_FrontFacing in ink shader; strips need mirrored back layer; no CJK fonts in sandbox; never .length=0 on live arrays; intro branch has its own anti-eclipse fade (M106) - keep it; File preview leases 15 min; set-viewport is lease-wide (verify innerWidth, reload page after).

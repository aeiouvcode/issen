# HANDOFF
Pipeline unchanged. Ground shader lives in index.html ~line 382 (groundMat fragment) and game-main.ts ~line 140 - same strings in both. Grass swath generator ~line 806. grep BRE quirk: patterns with ( ) * need python exact-match, not grep -c.
M101 candidates: dash smoke puffs (sprite burst on doDodge + boss movement), character edge dryness, real-phone prInfo read (ask Naksh what fps feel is). Reference frames at /tmp/refframes (ref-6 = best ground/stipple reference).

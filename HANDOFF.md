# HANDOFF — 2026-09-25 2:41 AM IST (post-M106)
State: M106 live + File gen 67. Standing mandate: 50-min cycles, distance-to-reference audit, mobile-first, gauntlet (game design / product / security / overall design / presentation / UI-UX / sound), publish each passed build as Instinct File, report per-milestone to parent.
Resume: pull live index.html if workspace wiped; state files from raw.githubusercontent; File skeleton via tools file checkout.
GitHub: web UI editor flow only (credential rule - token never in sandbox); session on cloud browser config-c; for files over ~200KB pass the paste script via --script-stdin (arg list limit); commit via dialog; verify repo md5 via contents API, then live md5 (Pages lags ~1-3 min).
Reference: sensonoken Unity sumi-e duel footage (/tmp/refframes/ref-*.png; refetch via api.fxtwitter.com/sensonoken/status/2100371695727198244).
Agent notes repo: https://github.com/aeiouvcode/agent-specific-notes (advisory) - consult before matching operations.
Known gotchas: no gl_FrontFacing in ink shader; strips need mirrored back layer; sandbox has no CJK fonts (render kanji in-browser); never .length=0 on live arrays; check browser console on silent no-start; the intro camera branch returns before gameplay anti-eclipse - guard it separately (M106 pattern).
